# Local test/print artifacts (large PDFs generated during manual verification)
test-artifacts/

# Secrets / local environment — never commit real credentials
.env
.env.*
!.env.example

# Local credential / password notes — never commit
supabase/Password_To_Okoudy_Project_on_Supabase.txt
*[Pp]assword*.txt
*[Ss]ecret*.txt

# Node (no dependencies today, but keep CI/tooling output out of git)
node_modules/
npm-debug.log*

# Cloudflare / Wrangler local state
.wrangler/
.cloudflare/

# Supabase CLI local state
supabase/.branches/
supabase/.temp/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
